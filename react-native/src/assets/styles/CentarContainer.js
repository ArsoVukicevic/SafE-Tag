import { StyleSheet } from 'react-native';

const centarConStyles = StyleSheet.create({
    container: {
        flex: 1,
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#F6F6F6',
    },
});

export default centarConStyles;
